#include <bits/stdc++.h>
using namespace std;
class Node
{
public:
    int data;
    Node *p;

    class LinkedList
    {
    private:
        Node *first;

    private:
        LinkedList() { first = NULL; }
        LinkedList(int A[], int n) ~LinkedList
    };
}